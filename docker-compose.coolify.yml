# Docker Compose for Coolify deployments.
#
# This file joins Coolify's predefined internal network and intentionally does
# not publish host ports. Expose port 8080 in Coolify's domain/proxy settings
# and access the service internally as:
#
#   http://searxng:8080

name: searxng

services:
  searxng:
    image: ghcr.io/searxng/searxng:${SEARXNG_VERSION:-latest}
    restart: unless-stopped
    depends_on:
      valkey:
        condition: service_started
    expose:
      - "8080"
    networks:
      - coolify
    environment:
      SEARXNG_BIND_ADDRESS: 0.0.0.0
      SEARXNG_PORT: 8080
      SEARXNG_BASE_URL: ${SEARXNG_BASE_URL:-}
      SEARXNG_VALKEY_URL: valkey://valkey:6379/0
    volumes:
      - searxng-config:/etc/searxng
      - searxng-cache:/var/cache/searxng
    healthcheck:
      test:
        - CMD-SHELL
        - wget -q --spider http://127.0.0.1:8080/ || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  valkey:
    image: ghcr.io/valkey-io/valkey:9-alpine
    command: valkey-server --save 30 1 --loglevel warning
    restart: unless-stopped
    expose:
      - "6379"
    networks:
      - coolify
    volumes:
      - valkey-data:/data
    healthcheck:
      test:
        - CMD
        - valkey-cli
        - ping
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  searxng-config:
  searxng-cache:
  valkey-data:

networks:
  coolify:
    external: true
    name: ${COOLIFY_NETWORK:-coolify}
